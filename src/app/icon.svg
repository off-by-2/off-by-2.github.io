<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <rect width="512" height="512" rx="128" fill="#1f3d2b"/>
  <g transform="translate(256, 256)">
    <!-- Top Pill (Brand Lime) -->
    <rect x="-32" y="-140" width="64" height="96" rx="32" fill="#d1f07b" />
    <!-- Bottom Pill (Muted Forest) -->
    <rect x="-32" y="44" width="64" height="96" rx="32" fill="#3d5f4d" />
    <!-- Left Pill (Muted Forest) -->
    <rect x="-140" y="-32" width="96" height="64" rx="32" fill="#3d5f4d" />
    <!-- Right Pill (Muted Forest) -->
    <rect x="44" y="-32" width="96" height="64" rx="32" fill="#3d5f4d" />
    <!-- Center Core (White) -->
    <circle cx="0" cy="0" r="32" fill="#ffffff" />
  </g>
</svg>
